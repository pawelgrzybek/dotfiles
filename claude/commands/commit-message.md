---
description: "Create a commit message for staged files"
allowed-tools: Bash(git *)
---

## Context:

- Current git status: !`git status`
- Current git diff: !`git diff --staged`

## Task

Analyse staged files, and create a descriptive commit message. Do not auto-commit on my behalf, wait for approval and commit only when approved.
